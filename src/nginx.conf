worker_processes 1;

events {
  worker_connections 1024;
}

http {
  upstream backend {
    server backend-1:8080;
    server backend-2:8080;
  }

  server {
    listen 80;

    location / {
      proxy_pass http://backend;
    }
  }
}